import React from 'react';
export default function CampaignStrategy({strategy}){
 if(!strategy)return <p className="text-sm text-muted-foreground">Після запуску спочатку буде збережено стратегію, потім сценарії — по два за крок.</p>;
 return <details className="panel p-5" open><summary className="font-semibold cursor-pointer">Стратегія кампанії</summary><p className="text-sm whitespace-pre-wrap mt-4">{strategy.summary}</p><div className="grid md:grid-cols-2 gap-5 mt-5"><div><h3 className="text-sm font-semibold">Рубрики</h3>{strategy.pillars?.map(p=><p key={p.name} className="text-xs mt-2"><strong>{p.name}</strong> — {p.objective}</p>)}</div><div><h3 className="text-sm font-semibold">Співвідношення форматів</h3>{strategy.formats?.map(p=><p key={p.name} className="text-xs mt-2">{p.name} · {p.share}%</p>)}</div><div><h3 className="text-sm font-semibold">Тематичні серії</h3>{strategy.series?.map(p=><p key={p.name} className="text-xs mt-2"><strong>{p.name}</strong> — {p.progression}</p>)}</div><div><h3 className="text-sm font-semibold">Розвиток тем</h3>{strategy.journey?.map((p,i)=><p key={i} className="text-xs mt-2">Тиждень {p.week}: {p.focus}</p>)}</div></div><p className="mt-5 text-sm text-muted-foreground whitespace-pre-wrap">{strategy.bot_rationale}</p></details>;
}
import React from 'react';
export default function CampaignSceneEditor({scenes,onChange,disabled}){
 const update=(index,key,value)=>onChange(scenes.map((s,i)=>i===index?{...s,[key]:value}:s));
 return <div className="space-y-3">{scenes.map((s,i)=><fieldset key={i} disabled={disabled} className="rounded-lg border border-border p-3 grid sm:grid-cols-2 gap-3"><legend className="text-xs px-1">Сцена {i+1}{s.is_ad?' · реклама':''}</legend><label className="studio-field">Тривалість, с<input className="studio-input" required type="number" min="0.1" max="90" step="0.1" value={s.seconds} onChange={e=>update(i,'seconds',Number(e.target.value))}/></label><label className="studio-field">Текст на екрані<input className="studio-input" maxLength={200} value={s.screen_text} onChange={e=>update(i,'screen_text',e.target.value)}/></label><label className="studio-field sm:col-span-2">Озвучка сцени<textarea className="studio-input" rows={3} maxLength={1800} value={s.narration} onChange={e=>update(i,'narration',e.target.value)}/></label><label className="studio-field sm:col-span-2">Візуальне завдання<textarea className="studio-input" rows={2} required maxLength={1000} value={s.visual} onChange={e=>update(i,'visual',e.target.value)}/></label></fieldset>)}</div>;
}